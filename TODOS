# refactors
[ ] tidy up description rendering

# features (for another day)

[ ] Create new rule directly in CLI

    Create a plan to implement a new feature which makes it possible to create a new rule directly from the CLI.
    
    It should basically work very similar to "split mode" (see categorise.ts).
    
    So, when running bin/money summary... finds an uncategorised transaction and it prompts the user to provide details (via inquirer prompt), the user can type "rule", which then enters into "rule mode" and asks the user a) what the rule should be and b) what category the rule applies to. Then, the whole thing gets saved in data/rules.json and the runtime rules are also updated and the uncategorised item is attempted to be categorised again and (if the user didn't screw up) categorise using the new rule.

[ ] Guess / propose category (based on description, existing data, etc.)